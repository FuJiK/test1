#include <stdio.h>

int main(int argc, char const *argv[]) {
  char kamoku;
  int ten;
  printf("科目を選択して下さい(k:国語、s:数学)　：\n" );
  scanf("%*c%c", &kamoku );

  if (kamoku == k) {
      if (/* condition */) {
        /* code */
      }else if (/* condition */) {
        /* code */
      }else{

      }
  }else if (/* condition */) {
      if (/* condition */) {
            /* code */
      }else if (/* condition */) {
            /* code */
      }else{

      }
  }else{

  }

  return 0;
}
